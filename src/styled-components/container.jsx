import styled from 'styled-components';

const DataDiv = styled.div`
    display: grid;
    justify-content: center;
`

const HeaderContainer = styled.div`
    display: inline-flex;
`

const Container = styled.div`
    display: inline-flex;
`

export { DataDiv, HeaderContainer, Container }